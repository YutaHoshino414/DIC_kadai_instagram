<div class="bg-light p-3 p-sm-5 my-4 rounded">
  <h1><%= @user.name %>のページ</h1>
  <p><%= image_tag @user.image.url(:thumb) %></p>
</div>


<p>ユーザー: <%= @user.name %></p>
<p>メールアドレス: <%= @user.email %></p>

<%= link_to '編集', edit_user_path(@user.id), class: "btn btn-lg, btn-primary" %><hr>
<p><%= link_to 'お気に入りした投稿一覧', like_user_path(@user.id) %></p>
<%= link_to "みんなの投稿を確認", pictures_path %>