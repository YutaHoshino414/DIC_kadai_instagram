<div class="bg-light p-3 p-sm-5 my-4 rounded">
  <h1>お気に入り一覧ページ</h1>
</div>

<h2>「いいね」した投稿：</h2>
  <% @favorites.each do |f| %>
    <ul>
      <li><%= f.user.name %>さんの投稿</li>
          <%= image_tag f.image.url %>
    </ul>
  <% end %><hr>
<%= link_to 'Myページに戻る', user_path(@user.id) %>
