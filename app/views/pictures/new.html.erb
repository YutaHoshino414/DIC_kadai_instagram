<h1>新規投稿</h1>
<%= form_with(model: @picture, local: true ) do |form| %>

  <div>
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div><br>
  <div>
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>
  <br>
  <%= form.submit '投稿する' %>
<% end %>
